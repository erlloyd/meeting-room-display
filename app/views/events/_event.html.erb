<div class="<%= event_classes(calendar, event) %>" style="<%= event_styles(event, previous_event) %>">
  <%= l(event.begin_time + 2.hours, format: :time_only) %> -
  <%= l(event.end_time + 2.hours, format: :time_only) %> |
  <%= event.summary %>
</div>
